# Next.js API Routes Style Guide - apps/web

## Core Principles
- Used for server-side logic and API proxies.
- Located in `src/app/api/`.
- Must export named HTTP method handlers (GET, POST, etc.).
- Use `NextResponse` for returning JSON data.
