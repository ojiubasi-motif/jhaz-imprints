# Global Styles Style Guide - apps/web

## Core Principles
- Primary styles in `src/globals.css`.
- Uses `@tailwind` directives for base, components, and utilities.
- Custom component classes (like `.btn-primary`, `.card`) defined with `@apply` to maintain consistency across the app.
